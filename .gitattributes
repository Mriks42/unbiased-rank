# Normalize line endings to LF in the repository regardless of platform.
# Without this, committing from Windows stores CRLF, which shows up as
# whole-file diffs when CI or a collaborator works on Linux.
* text=auto eol=lf

# Binary artifacts must never be line-ending converted.
*.parquet binary
*.joblib binary
*.png binary
*.jpg binary
*.pdf binary

.venv/
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/

# Data is not vendored — see configs/data.yaml for the source.
data/raw/
data/interim/
data/quarantine/

# Experiment outputs
mlruns/
outputs/

# ...but committed result files are the deliverable. A reader should be able to
# see the numbers without re-running a 25-minute encode, and diffs across
# commits make it visible when a result changes.
!outputs/
outputs/**
!outputs/*.json
!outputs/*.md
.hypothesis/
.pytest_cache/
.mypy_cache/
.ruff_cache/

.env
